        // This is my first JavaScript code!
 /*       console.log("hello world")
    let name = 'Yunus';
//    console.log(name) ;
*/
    // cannot be a reserved keyword
    // Should be meaningful
    // Cannot start with a number (1name)
    // Cannot contain a space or hyphen (-)
    // Aere case-sensitive 
/*
    let person = {
        name: "Yunus",
        age: 16 
    };

    // Dot Notations
    person.name = "Yunus";
    console.log(person.name) ; 

    //Bracket Notations
    person["name"] = "name";
    person[selection] = "Yunus";
    console.log(person.name);

    let selectedColors = ['red', 'blue']
    selectedColors[2] = 1;
    console.log(selectedColors.length);
    function greeet () {
        console.log('Hello World');
    }

    greet ("World")

    function greet(name) {
    console.log('hello ' + name);
    }
    greet('Yunus', 'Kilicalp');

    function best(yourName){
        console.log(yourName + ' is the best programmmer.');
    }
    best('Yunus');

    function greet(name, lastname){
        console,log('Hello + name + " + LastName')
    }

    function area(base,height){
        return base * height/2;
    }
    console.log(area 12/17); {

    for (let i = 0; i < 5; i++) {
        console.log('I am the best programmer');
   }

   for (let i = 0; i < 100; i++) {
    console.log('Hello World', i);
   }
*/
  /* for (let i = 0; i < 50; i++) {
    console.log(i)
   }
   function power(base, exponent){
    result=1;
    for (let i-0; i<exponent ; i++)
    result=result* base;
   }
   result
/*
console.log(power (2,4))

/*

function addup(start,end)
    result=0
    for (let i = begin; i<= finish; i++){
        result=result+i
        }
        return (result);
        } 
        console.log(addup(4,80))
*/
 /*       let counter=1;
        while (counter<101){
            console.log(counter)
            counter++
        }

        for (let i=1; i<101 ; i++){
            console.log(i)
        }
   */     

        let password="magnolia123"
        let userInput=null
        while(userinput =! password){
            userInput=prompt("please enter the right password")

        }
        alert ("Acess Granted!!")
            

    
    /*
        let myName=prompt("Please say your name")
        alert("Welcome "+myName+ " to your website")
    */
   
    /*    let hour = 10;

        if (hour >= 6 && hour < 12) {
           console.log('Good morning');
        }
        else if (hour >= 12 && hour < 17) {
            console.log('good afternoon')
        }
     else if (hour > 5 && hour < 8)
            console.log('good evening')
      else (hour >=9 )
        console.log('good night')
    */
    alert (useInput + 3)
    alert ("Acess Granted!!")
/*
        let age = prompt('what is your age');
    /*      if (age >= 1 && age < 4){
            alert('baby')
        }
 /*       else if (age >= 5 && age < 12){
            alert('brother')
        }
/*        else if (age >= 10 && age < 4)
              alert(smalll brother')
 /*